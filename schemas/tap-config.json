{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/jlrickert/tapper/main/schemas/tap-config.json",
  "title": "tapper user/project config",
  "description": "Schema for tapper user or project configuration.",
  "type": "object",
  "properties": {
    "keg": {
      "type": "string",
      "description": "KEG reference. Precedence: --keg, TAP_KEG, project keg, matching kegMap.keg, user keg."
    },
    "flight": {
      "type": "string",
      "description": "Flight reference (@namespace/+slug, +slug, or bare slug). Precedence: --flight, TAP_FLIGHT, project flight, matching kegMap.flight, user flight."
    },
    "agent": {
      "type": "string",
      "description": "Name of the entry in agents{} driving this process. `tap launch` uses it as the default when --agent is omitted, the same way flight supplies the launch root, and exports the resolved name as TAP_AGENT so the child can report its own identity. Selects a model and supplies telemetry only \u2014 flight selection is independent. Override with TAP_AGENT env var. Experimental: expected to change when agents move to the hub."
    },
    "hub": {
      "type": "string",
      "description": "Saved Hub name. Precedence: --hub, TAP_HUB, project hub, matching kegMap.hub, user hub, alphabetically first configured Hub, built-in Atlas unless disabled. All KEGs resolve within this Hub."
    },
    "disableAtlasHub": {
      "type": "boolean",
      "description": "When true, the synthesized built-in atlas hub (https://atlas.foldwise.ai) is suppressed: it is not synthesized, is omitted from hub listings, and is skipped in hub resolution, so hub-dependent commands fail with a clear error if no other hub is configured. An explicit atlas entry in hubs{} is unaffected. Override with TAP_DISABLE_ATLAS_HUB env var (1/true/yes/on)."
    },
    "disableTelemetry": {
      "type": "boolean",
      "description": "When true, disables privacy-minimized Tap CLI and MCP invocation reporting. Reporting is enabled by default and can also be disabled with TAP_DISABLE_TELEMETRY=1."
    },
    "kegMap": {
      "type": "array",
      "description": "Startup-directory defaults. First matching regex wins; otherwise longest directory prefix wins, with ties in configuration order. Exactly one rule supplies defaults beneath project config.",
      "items": {
        "type": "object",
        "description": "Any combination of KEG, Hub, and flight defaults. Omitted fields fall back independently to user config.",
        "properties": {
          "pathPrefix": {
            "type": "string",
            "description": "Directory prefix; matches the directory itself and descendants, never a partial directory name.",
            "minLength": 1
          },
          "pathRegex": {
            "type": "string",
            "description": "Regular expression used to match the current path.",
            "minLength": 1
          },
          "keg": {
            "type": "string",
            "minLength": 1,
            "description": "Optional KEG reference. Overridden by project keg, TAP_KEG, or --keg."
          },
          "hub": {
            "type": "string",
            "minLength": 1,
            "description": "Optional saved Hub name, overridden by project hub, TAP_HUB, or --hub."
          },
          "flight": {
            "type": "string",
            "description": "Optional flight reference, overridden by project flight, TAP_FLIGHT, or --flight. Empty does not clear an inherited flight."
          }
        },
        "additionalProperties": true,
        "allOf": [
          {
            "anyOf": [
              {
                "required": [
                  "keg"
                ]
              },
              {
                "required": [
                  "hub"
                ]
              },
              {
                "required": [
                  "flight"
                ],
                "properties": {
                  "flight": {
                    "minLength": 1
                  }
                }
              }
            ]
          },
          {
            "anyOf": [
              {
                "required": [
                  "pathPrefix"
                ]
              },
              {
                "required": [
                  "pathRegex"
                ]
              }
            ]
          }
        ]
      }
    },
    "hubs": {
      "type": "object",
      "description": "Saved Hub connections, allowed only in user configuration.",
      "additionalProperties": {
        "type": "object",
        "description": "Single hub configuration entry.",
        "properties": {
          "url": {
            "type": "string",
            "description": "Hub base URL."
          },
          "token": {
            "type": "string",
            "description": "Inline token credential for hub access."
          },
          "tokenEnv": {
            "type": "string",
            "description": "Environment variable name containing the hub token."
          }
        },
        "additionalProperties": true
      }
    },
    "agents": {
      "type": "object",
      "description": "Named model definitions for `tap launch`, keyed by alias. Agents hold no secrets \u2014 apiKeyEnv names a variable, never a value \u2014 so unlike hubs they survive the project-config trust strip and may safely be defined in project config. Experimental: expected to change when agents move to the hub.",
      "additionalProperties": {
        "type": "object",
        "description": "Single agent definition: a model plus how to reach and authenticate against it.",
        "properties": {
          "model": {
            "type": "string",
            "description": "Provider-qualified model, '<provider>/<model>' \u2014 anthropic/claude-opus-4, openai/gpt-5, ollama/qwen3.6:35b. The prefix tells the launcher which protocol the harness must speak; recognized providers are anthropic, openai, and ollama."
          },
          "baseUrl": {
            "type": "string",
            "description": "Overrides the provider's endpoint. One value serves both protocols: the launcher appends /v1 for OpenAI clients and trims it for Anthropic ones. Defaults to http://localhost:11434/v1 for ollama models; empty for hosted providers, leaving the harness on its own endpoint."
          },
          "auth": {
            "type": "string",
            "enum": [
              "inherit",
              "subscription",
              "apiKey",
              "none"
            ],
            "description": "Where the harness gets credentials. 'inherit' passes the ambient environment through untouched. 'subscription' strips inherited provider key variables so the harness falls back to its own stored login. 'apiKey' forwards the variable named by apiKeyEnv. 'none' strips the same variables but implies no stored login to fall back to. When omitted the mode is derived: apiKey if apiKeyEnv is set, else none for ollama models, else inherit."
          },
          "apiKeyEnv": {
            "type": "string",
            "description": "Name of the environment variable holding the API key \u2014 the name is configured, never the secret, mirroring hubs.*.tokenEnv. Setting it also selects auth 'apiKey' when auth is omitted."
          },
          "contextWindow": {
            "type": "integer",
            "minimum": 1,
            "description": "Caps the working context in tokens. Harnesses express this differently \u2014 Codex as model metadata, Claude Code as an auto-compact threshold \u2014 so the launcher translates it per harness, and launching one with no equivalent is an error rather than a silent drop."
          },
          "args": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Extra arguments passed to the harness. Placed before any arguments given after `--` on the tap launch command line, so a one-off invocation can override them."
          }
        },
        "required": [
          "model"
        ],
        "additionalProperties": true
      }
    },
    "relay": {
      "type": "object",
      "description": "Configures `tap relay`: which local or personal model providers this machine contributes to Hub's model catalog. Holds provider access and nothing else — model identity comes from the Hub catalog. Allowed only in user configuration; project layers are stripped.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "false makes `tap relay` refuse to start while keeping the rest of the block. Omitted means enabled."
        },
        "name": {
          "type": "string",
          "pattern": "^[A-Za-z0-9._-]{1,64}$",
          "description": "Identifies the relay to Hub and prefixes its catalog ids (<relay>/<provider>/<model>). Defaults to a name derived from the hostname; `tap relay --name` overrides it."
        },
        "hubs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Names from hubs to serve at once, each over its own connection with the same providers. Omitted means the one hub login resolution picks; `tap relay --hub` narrows to one."
        },
        "providers": {
          "type": "object",
          "description": "Model providers the relay reaches, keyed by the provider name advertised to Hub.",
          "propertyNames": {
            "pattern": "^[A-Za-z0-9._-]{1,64}$"
          },
          "additionalProperties": {
            "type": "object",
            "description": "How the relay reaches one model provider.",
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "ollama",
                  "openai",
                  "openrouter",
                  "openai-compatible"
                ],
                "description": "Provider adapter. Defaults to the provider's key, so a provider named ollama needs no kind."
              },
              "baseUrl": {
                "type": "string",
                "pattern": "^https?://",
                "description": "OpenAI-compatible API base URL, including /v1. Defaults per kind: http://127.0.0.1:11434/v1 for ollama, https://api.openai.com/v1 for openai, https://openrouter.ai/api/v1 for openrouter. Required for openai-compatible."
              },
              "auth": {
                "type": "string",
                "enum": [
                  "none",
                  "apiKey"
                ],
                "description": "How the relay authenticates to the provider. 'none' sends no credential (local providers such as Ollama). 'apiKey' sends the key read from the variable named by apiKeyEnv. Defaults to apiKey for openai and openrouter, none otherwise."
              },
              "apiKeyEnv": {
                "type": "string",
                "description": "Name of the environment variable holding the API key — the name is configured, never the secret, mirroring hubs.*.tokenEnv. Defaults to OPENAI_API_KEY for openai and OPENROUTER_API_KEY for openrouter. The key stays in the relay process and never reaches Hub."
              },
              "maxConcurrent": {
                "type": "integer",
                "minimum": 1,
                "maximum": 64,
                "description": "This provider's in-flight requests across every hub the relay serves. Defaults to 4."
              },
              "priority": {
                "type": "integer",
                "minimum": 1,
                "maximum": 99,
                "description": "This provider's rank on Hub, lower preferred, both in the model list and when a pool picks a source. Omitted means unranked, after every ranked provider."
              },
              "models": {
                "type": "object",
                "description": "Which of the provider's models are offered to Hub, resolved against the provider's own model list.",
                "properties": {
                  "allow": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Model ids or path.Match glob patterns (e.g. qwen3*) to offer. Empty offers every model not denied."
                  },
                  "deny": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Model ids or path.Match glob patterns never to offer. Deny wins over allow."
                  },
                  "transcription": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Model ids or path.Match glob patterns that turn speech into text through the provider's /audio/transcriptions endpoint. Offered for dictation, not chat; exact ids are offered even when the provider's /models omits them."
                  }
                },
                "additionalProperties": true
              }
            },
            "additionalProperties": true
          }
        }
      },
      "additionalProperties": true
    },
    "logFile": {
      "type": "string",
      "description": "Path to the log output file. Override with TAP_LOG_FILE env var."
    },
    "logLevel": {
      "type": "string",
      "description": "Log verbosity level (debug, info, warn, error). Override with TAP_LOG_LEVEL env var."
    }
  },
  "additionalProperties": true
}
