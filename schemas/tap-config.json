{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/jlrickert/tapper/main/schemas/tap-config.json",
  "title": "tapper user/project config",
  "description": "Schema for tapper user or project configuration.",
  "type": "object",
  "properties": {
    "defaultKeg": {
      "type": "string",
      "description": "Alias used when no explicit keg is requested. Override with TAP_DEFAULT_KEG env var."
    },
    "fallbackKeg": {
      "type": "string",
      "description": "Last-resort keg alias used when no mapped or default alias resolves. Override with TAP_FALLBACK_KEG env var."
    },
    "defaultHub": {
      "type": "string",
      "description": "High-precedence hub name used when a keg reference omits its hub. The authoritative choice; set it in project config. Override with TAP_DEFAULT_HUB env var."
    },
    "fallbackHub": {
      "type": "string",
      "description": "Last-resort hub name used when a keg reference omits its hub and no defaultHub applies. Set it in the global user config so references need not specify a hub. Override with TAP_FALLBACK_HUB env var."
    },
    "defaultNamespace": {
      "type": "string",
      "description": "High-precedence namespace used when a keg reference omits its namespace. Set it in project config. Override with TAP_DEFAULT_NAMESPACE env var."
    },
    "fallbackNamespace": {
      "type": "string",
      "description": "Last-resort namespace used when a keg reference omits its namespace and no defaultNamespace applies. Set it in the global user config. Override with TAP_FALLBACK_NAMESPACE env var."
    },
    "disableDefaultHub": {
      "type": "boolean",
      "description": "When true, the compiled-in DefaultHubURL fallback (https://atlas.foldwise.ai) is suppressed and hub-dependent commands fail with a clear error if no other hub is configured. Override with TAP_DISABLE_DEFAULT_HUB env var (1/true/yes/on)."
    },
    "kegs": {
      "type": "object",
      "description": "Map of alias names to (hub, namespace, name) keg references.",
      "additionalProperties": {
        "type": "object",
        "description": "A keg reference. Use the (hub, namespace, name) triple; an empty hub falls back to defaultHub/fallbackHub and an empty namespace falls back to defaultNamespace/fallbackNamespace. 'path' is a legacy/explicit local-filesystem escape hatch that takes precedence over the triple.",
        "properties": {
          "hub": {
            "type": "string",
            "description": "Hub name this keg lives on. Empty uses defaultHub then fallbackHub."
          },
          "namespace": {
            "type": "string",
            "description": "Namespace owner. The @ sigil is implied; do not include it. Empty uses defaultNamespace then fallbackNamespace."
          },
          "name": {
            "type": "string",
            "description": "Keg name within the namespace."
          },
          "path": {
            "type": "string",
            "description": "Explicit local filesystem path. When set it takes precedence over the triple (legacy/escape hatch)."
          }
        },
        "additionalProperties": false
      }
    },
    "kegMap": {
      "type": "array",
      "description": "Path-based rules that map files to keg aliases.",
      "items": {
        "type": "object",
        "description": "Single path-to-alias mapping rule.",
        "properties": {
          "alias": {
            "type": "string",
            "description": "Keg alias selected when this rule matches."
          },
          "pathPrefix": {
            "type": "string",
            "description": "Path prefix that must match the current path."
          },
          "pathRegex": {
            "type": "string",
            "description": "Regular expression used to match the current path."
          }
        },
        "required": ["alias"],
        "additionalProperties": false
      }
    },
    "hubs": {
      "type": "object",
      "description": "Map of hub name to hub definition. A hub is a backend kegs are addressed against.",
      "additionalProperties": {
        "type": "object",
        "description": "Single hub configuration entry.",
        "properties": {
          "kind": {
            "type": "string",
            "enum": ["remote", "local", "readonly"],
            "description": "Hub backend kind: 'remote' (read-write HTTP, the default when omitted), 'local' (filesystem on this machine), or 'readonly' (read-only HTTP)."
          },
          "url": {
            "type": "string",
            "description": "Base URL for remote/readonly hubs."
          },
          "basePath": {
            "type": "string",
            "description": "Filesystem root for local hubs. Kegs resolve to <basePath>/<name>."
          },
          "token": {
            "type": "string",
            "description": "Inline token credential for hub access."
          },
          "tokenEnv": {
            "type": "string",
            "description": "Environment variable name containing the hub token."
          }
        },
        "additionalProperties": false
      }
    },
    "logFile": {
      "type": "string",
      "description": "Path to the log output file. Override with TAP_LOG_FILE env var."
    },
    "logLevel": {
      "type": "string",
      "description": "Log verbosity level (debug, info, warn, error). Override with TAP_LOG_LEVEL env var."
    },
    "updated": {
      "type": "string",
      "description": "RFC3339 timestamp for the last config update.",
      "format": "date-time"
    }
  },
  "additionalProperties": true
}
