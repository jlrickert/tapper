{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/jlrickert/tapper/main/schemas/flight-manifest.json",
  "title": "tapper flight manifest",
  "description": "Schema for a tapper flight manifest edited by `tap flight edit`.",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Human-readable title for the flight."
    },
    "visibility": {
      "type": "string",
      "enum": ["private", "public"],
      "default": "private",
      "description": "Flight discovery visibility."
    },
    "capabilities": {
      "type": "array",
      "items": {"type": "string", "enum": ["full_access", "manage_flights", "manage_kegs"]},
      "uniqueItems": true,
      "description": "Explicit agent capabilities granted to sessions using this flight."
    },
    "cover": {
      "type": "array",
      "description": "Kegs available while the flight is active. Empty or omitted denies all KEG access.",
      "items": {
        "type": "object",
        "properties": {
          "namespace": {
            "type": "string",
            "description": "Namespace containing the keg. The @ sigil is optional."
          },
          "keg": {
            "type": "string",
            "description": "Keg name covered by this flight entry."
          },
          "role": {
            "type": "string",
            "description": "Maximum role granted by this cover entry. Omitted roles default to viewer.",
            "enum": ["viewer", "editor", "admin"],
            "default": "viewer"
          }
        },
        "required": ["keg"],
        "additionalProperties": false
      }
    },
    "subflights": {
      "type": "array",
      "description": "Ordered child relations in a bounded recursive graph. Each selected descendant keeps independent instructions and authority.",
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "^(?:@[a-z0-9][a-z0-9-]{0,63}/)?\\+[a-z0-9][a-z0-9-]{0,63}$"
      },
      "uniqueItems": true,
      "maxItems": 64
    },
    "allowedKegs": {
      "type": "array",
      "description": "Legacy local-manifest cover list. Bare entries are treated as editor cover rows for backward compatibility.",
      "items": {
        "type": "string"
      }
    },
    "instructions": {
      "type": "string",
      "description": "Markdown instructions injected into orientation output while the flight is active."
    }
  },
  "additionalProperties": false
}
