{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/jlrickert/tapper/main/schemas/keg-config.json",
  "title": "tapper keg config",
  "description": "Schema for keg metadata stored in a keg repository config file.",
  "type": "object",
  "properties": {
    "kegv": {
      "type": "string",
      "description": "Configuration schema version.",
      "enum": ["2025-07"]
    },
    "updated": {
      "type": "string",
      "description": "RFC3339 timestamp indicating when the keg metadata was last updated.",
      "format": "date-time"
    },
    "title": {
      "type": "string",
      "description": "Human-readable title for this keg."
    },
    "url": {
      "type": "string",
      "description": "Primary URL for the keg or project."
    },
    "creator": {
      "type": "string",
      "description": "Creator identifier, profile URL, or repository URL."
    },
    "state": {
      "type": "string",
      "description": "Lifecycle state for the keg, such as living or archived."
    },
    "summary": {
      "type": "string",
      "description": "Short summary of the keg's purpose and contents."
    },
    "links": {
      "type": "array",
      "description": "Named external links or related keg references.",
      "items": {
        "type": "object",
        "description": "A single named link entry.",
        "properties": {
          "alias": {
            "type": "string",
            "description": "Unique alias used to reference the link."
          },
          "url": {
            "type": "string",
            "description": "Destination URL or keg target."
          }
        },
        "required": ["alias", "url"],
        "additionalProperties": true
      }
    },
    "indexes": {
      "type": "array",
      "description": "Configured index outputs and optional tag filters.",
      "items": {
        "type": "object",
        "description": "A single index declaration.",
        "properties": {
          "file": {
            "type": "string",
            "description": "Bare filename of the generated index artifact (e.g. \"backlinks\", \"concepts.md\"); the on-disk path is always under the keg's dex/ directory and the prefix is applied at write time."
          },
          "summary": {
            "type": "string",
            "description": "Short description of what this index contains."
          },
          "query": {
            "type": "string",
            "description": "Boolean query expression to filter index contents. Supports tag names, key=value attribute predicates, dot-prefix stats fields (.created>2026-01-01, .accessCount>=5, .omega>=0.8), and boolean operators (and/or/not)."
          },
          "sort": {
            "type": "string",
            "description": "Sort order for query-filtered index entries.",
            "enum": ["id", "updated", "created", "accessed"],
            "default": "updated"
          }
        },
        "required": ["file", "summary"],
        "additionalProperties": true
      }
    },
    "entities": {
      "type": "object",
      "description": "Entity map keyed by entity name.",
      "additionalProperties": {
        "type": "object",
        "description": "Metadata for a single entity.",
        "properties": {
          "id": {
            "type": "integer",
            "description": "Node ID associated with the entity.",
            "minimum": 0
          },
          "summary": {
            "type": "string",
            "description": "Short description of the entity."
          }
        },
        "required": ["id"],
        "additionalProperties": false
      }
    },
    "tags": {
      "type": "object",
      "description": "Tag descriptions keyed by tag name.",
      "additionalProperties": {
        "type": "string",
        "description": "Human-readable description for a tag."
      }
    },
    "timezone": {
      "type": "string",
      "description": "IANA timezone for resolving ambiguous timestamps within this keg (e.g. America/Chicago). Defaults to UTC.",
      "default": "UTC"
    },
    "snapshots": {
      "type": "object",
      "description": "Per-keg automatic snapshot policy.",
      "properties": {
        "mode": {
          "type": "string",
          "description": "Automatic snapshot mode. \"auto\" lets hub servers snapshot idle changed nodes; \"off\" disables automatic snapshots for this keg.",
          "enum": ["auto", "off"],
          "default": "auto"
        },
        "idleAfter": {
          "type": "string",
          "description": "Idle window before an automatic snapshot is eligible, formatted as a Go duration such as 1h or 30m.",
          "default": "1h"
        }
      },
      "additionalProperties": false,
      "default": {
        "mode": "auto",
        "idleAfter": "1h"
      }
    },
    "doctor": {
      "type": "object",
      "description": "Configuration for `tap doctor` health checks.",
      "properties": {
        "entityCheck": {
          "type": "boolean",
          "description": "Enable per-node entity attribute validation. When true, doctor warns about nodes missing an entity attribute in metadata. Default: false.",
          "default": false
        },
        "tagCheck": {
          "type": "boolean",
          "description": "Enable per-node tag validation against the keg config tag map. When true, doctor warns about tags used in node metadata that are not documented in the keg config. Default: false.",
          "default": false
        }
      },
      "additionalProperties": false
    },
    "site": {
      "type": "object",
      "description": "Static site generation defaults for `tap site`.",
      "properties": {
        "output": {
          "type": "string",
          "description": "Default output directory for the generated site.",
          "default": "./site"
        },
        "title": {
          "type": "string",
          "description": "Site title shown in navigation and page titles."
        },
        "baseUrl": {
          "type": "string",
          "description": "Base URL prefix for absolute links (e.g. https://keg.example.com/).",
          "default": "/"
        },
        "search": {
          "type": "boolean",
          "description": "Enable client-side search via Pagefind. Set to false to disable.",
          "default": true
        }
      },
      "additionalProperties": false
    }
  },
  "required": ["kegv"],
  "additionalProperties": true
}
