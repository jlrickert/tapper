# CLAUDE.md

Read [AGENTS.md](./AGENTS.md). It is the entry point for every agent and
contributor in this repository: the workflow, the rules that are expensive to
get wrong, and the table of which reference to read for which surface.

Detail lives under [`docs/`](docs/), which is the source of truth. Keep it
there — this file stays a pointer so there is one place to edit.
